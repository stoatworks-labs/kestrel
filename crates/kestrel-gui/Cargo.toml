[package]
name = "kestrel-gui"
description = "Kestrel's operator window: a large input preview with draggable regions, a row of output previews, and a crosspoint matrix."
version.workspace = true
edition.workspace = true
license.workspace = true
authors.workspace = true
repository.workspace = true

[[bin]]
name = "kestrel-gui"
path = "src/main.rs"

[dependencies]
kestrel-core = { path = "../kestrel-core" }
kestrel-render = { path = "../kestrel-render" }
kestrel-decklink = { path = "../kestrel-decklink" }
kestrel-control = { path = "../kestrel-control" }
kestrel-app = { path = "../kestrel-app" }
diag = { path = "../diag" }

egui.workspace = true
eframe = "0.36"
anyhow.workspace = true
tokio.workspace = true
tracing.workspace = true
